[build]
  command = "npm run build"
  publish = "dist"
  [build.environment]
    NODE_VERSION = "20.11.1"
    NPM_FLAGS = "--no-fund --no-audit"

# NOTE: Les règles Redirects/Headers sont fournies par public/_headers et public/_redirects
